# This is a template file for environment variables used by the API server.

# Port the Express server will run on
PORT=5000

# PostgreSQL connection string
DATABASE_URL=postgresql://postgres:password@db:5432/pizza-app

# Secret key used to sign JWTs for authentication
JWT_SECRET=your_jwt_secret_here

# Allowed origin for CORS
CLIENT_URL=http://localhost:3000

# Optional: Set to "development" or "production"
# NODE_ENV=development
